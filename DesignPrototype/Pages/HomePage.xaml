<Page x:Class="DesignPrototype.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DesignPrototype.Pages">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#1E1E1E" CornerRadius="8" Margin="10">
                <Image Source="/Assets/pley.jpg" Stretch="Fill" />
            </Border>
            <Border Grid.Row="1" Background="#1E1E1E" CornerRadius="8" Margin="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <!-- Заголовок (фиксированный) -->
                        <RowDefinition Height="Auto"/>
                        <!-- Новости (занимают всё остальное место) -->
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 📰 ЗАГОЛОВОК (фиксированный, НЕ скроллится) -->
                    <TextBlock Grid.Row="0"
                   Text="📰 Лента новостей" 
                   FontSize="14"
                   FontWeight="Bold" 
                   Foreground="White"
                   Margin="15,15,15,10"/>

                    <!-- 📜 СКРОЛЛЕР (только для новостей) -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden"  HorizontalScrollBarVisibility="Disabled" Margin="15,0,15,15">
                        <StackPanel>
                            <Border Background="#2A2A2A" CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="• ЦБ РФ сохранил ключевую ставку на уровне 16%" 
                                   Foreground="#F44336" TextWrapping="Wrap" FontSize="12"/>
                                    <TextBlock Text="14:32" Foreground="#666666" FontSize="10" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#2A2A2A" CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="• Индекс Мосбиржи вырос на 1.5% до 3250 пунктов" 
                                   Foreground="#4CAF50" TextWrapping="Wrap" FontSize="12"/>
                                    <TextBlock Text="13:15" Foreground="#666666" FontSize="10" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#2A2A2A" CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="• Сбербанк отчитался о прибыли за квартал +12%" 
                                   Foreground="#B0B0B0" TextWrapping="Wrap" FontSize="12"/>
                                    <TextBlock Text="12:00" Foreground="#666666" FontSize="10" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#2A2A2A" CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="• Нефть Brent торгуется на уровне $82.50 за баррель" 
                                   Foreground="#B0B0B0" TextWrapping="Wrap" FontSize="12"/>
                                    <TextBlock Text="11:30" Foreground="#666666" FontSize="10" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#2A2A2A" CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="• ЦБ РФ сохранил ключевую ставку на уровне 16%" 
                                   Foreground="#F44336" TextWrapping="Wrap" FontSize="12"/>
                                    <TextBlock Text="14:32" Foreground="#666666" FontSize="10" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#2A2A2A" CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="• Индекс Мосбиржи вырос на 1.5% до 3250 пунктов" 
                                   Foreground="#4CAF50" TextWrapping="Wrap" FontSize="12"/>
                                    <TextBlock Text="13:15" Foreground="#666666" FontSize="10" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#2A2A2A" CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="• Сбербанк отчитался о прибыли за квартал +12%" 
                                   Foreground="#B0B0B0" TextWrapping="Wrap" FontSize="12"/>
                                    <TextBlock Text="12:00" Foreground="#666666" FontSize="10" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                            <Border Background="#2A2A2A" CornerRadius="4" Padding="10" Margin="0,0,0,8">
                                <StackPanel>
                                    <TextBlock Text="• Нефть Brent торгуется на уровне $82.50 за баррель" 
                                   Foreground="#B0B0B0" TextWrapping="Wrap" FontSize="12"/>
                                    <TextBlock Text="11:30" Foreground="#666666" FontSize="10" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- 📄 ПРАВАЯ КОЛОНКА (разделена на 2 ряда) -->
        <Grid Grid.Column="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="70*"/> <!-- Row 0: Виджеты (70%) -->
                <RowDefinition Height="30*"/> <!-- Row 1: Портфель + Биржи (30%) -->
            </Grid.RowDefinitions>

            <!-- ═══════════════════════════════════════════════════════════ -->
            <!--  ROW 0: ТАБЛИЦЫ ЛИДЕРОВ                                     -->
            <!-- ═══════════════════════════════════════════════════════════ -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60*"/>
                    <!-- 3 таблицы лидеров -->
                    <RowDefinition Height="5"/>
                    <!-- Разделитель -->
                    <RowDefinition Height="40*"/>
                    <!-- Избранное, Курсы, Статистика, Календарь -->
                </Grid.RowDefinitions>

                <!-- ═══════════════════════════════════════════════════════ -->
                <!--  3 ТАБЛИЦЫ ЛИДЕРОВ                                      -->
                <!-- ═══════════════════════════════════════════════════════ -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- ═══════════════════════════════════════════════════ -->
                    <!--  ТАБЛИЦА 1: ЛИДЕРЫ ПО ОБОРОТУ                       -->
                    <!-- ═══════════════════════════════════════════════════ -->
                    <Border Grid.Column="0" Background="#1E1E1E" CornerRadius="4">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="📊 Лидеры по обороту" 
                               FontSize="12" FontWeight="Bold" 
                               Foreground="White"
                               Margin="10,8,10,5"/>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" Margin="10,0,10,8">
                                <DataGrid AutoGenerateColumns="False"
                                  HeadersVisibility="Column"
                                  Background="#1E1E1E"
                                  RowBackground="#1E1E1E"
                                  AlternatingRowBackground="#252525"
                                  Foreground="White"
                                  BorderThickness="0"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#404040"
                                  FontSize="11"
                                  FontFamily="Consolas"
                                  CanUserAddRows="False"
                                  IsReadOnly="True">
                                    <DataGrid.ColumnHeaderStyle>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="Background" Value="#2A2A2A"/>
                                            <Setter Property="Foreground" Value="#90CAF9"/>
                                            <Setter Property="Padding" Value="5,4"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="FontSize" Value="10"/>
                                        </Style>
                                    </DataGrid.ColumnHeaderStyle>
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Код" Width="50"/>
                                        <DataGridTextColumn Header="Инструмент" Width="70"/>
                                        <DataGridTextColumn Header="Оборот" Width="80"/>
                                    </DataGrid.Columns>
                                    <DataGrid.RowStyle>
                                        <Style TargetType="DataGridRow">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsUp}" Value="True">
                                                    <Setter Property="Background" Value="#1B5E20"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsUp}" Value="False">
                                                    <Setter Property="Background" Value="#B71C1C"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGrid.RowStyle>
                                </DataGrid>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <!-- РАЗДЕЛИТЕЛЬ -->
                    <Border Grid.Column="1" Background="#404040" Margin="0,5"/>

                    <!-- ═══════════════════════════════════════════════════ -->
                    <!--  ТАБЛИЦА 2: ЛИДЕРЫ РОСТА                            -->
                    <!-- ═══════════════════════════════════════════════════ -->
                    <Border Grid.Column="2" Background="#1E1E1E" CornerRadius="4">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="📈 Лидеры роста" 
                               FontSize="12" FontWeight="Bold" 
                               Foreground="White"
                               Margin="10,8,10,5"/>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" Margin="10,0,10,8">
                                <DataGrid AutoGenerateColumns="False"
                                  HeadersVisibility="Column"
                                  Background="#1E1E1E"
                                  RowBackground="#1E1E1E"
                                  AlternatingRowBackground="#252525"
                                  Foreground="White"
                                  BorderThickness="0"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#404040"
                                  FontSize="11"
                                  FontFamily="Consolas"
                                  CanUserAddRows="False"
                                  IsReadOnly="True">
                                    <DataGrid.ColumnHeaderStyle>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="Background" Value="#2A2A2A"/>
                                            <Setter Property="Foreground" Value="#90CAF9"/>
                                            <Setter Property="Padding" Value="5,4"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="FontSize" Value="10"/>
                                        </Style>
                                    </DataGrid.ColumnHeaderStyle>
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Код" Width="50"/>
                                        <DataGridTextColumn Header="Инструмент" Width="70"/>
                                        <DataGridTextColumn Header="% измен." Width="60"/>
                                    </DataGrid.Columns>
                                    <DataGrid.RowStyle>
                                        <Style TargetType="DataGridRow">
                                            <Setter Property="Background" Value="#1B5E20"/>
                                        </Style>
                                    </DataGrid.RowStyle>
                                </DataGrid>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <!-- РАЗДЕЛИТЕЛЬ -->
                    <Border Grid.Column="3" Background="#404040" Margin="0,5"/>

                    <!-- ═══════════════════════════════════════════════════ -->
                    <!--  ТАБЛИЦА 3: ЛИДЕРЫ ПАДЕНИЯ                          -->
                    <!-- ═══════════════════════════════════════════════════ -->
                    <Border Grid.Column="4" Background="#1E1E1E" CornerRadius="4">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="📉 Лидеры падения" 
                               FontSize="12" FontWeight="Bold" 
                               Foreground="White"
                               Margin="10,8,10,5"/>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" Margin="10,0,10,8">
                                <DataGrid AutoGenerateColumns="False"
                                  HeadersVisibility="Column"
                                  Background="#1E1E1E"
                                  RowBackground="#1E1E1E"
                                  AlternatingRowBackground="#252525"
                                  Foreground="White"
                                  BorderThickness="0"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#404040"
                                  FontSize="11"
                                  FontFamily="Consolas"
                                  CanUserAddRows="False"
                                  IsReadOnly="True">
                                    <DataGrid.ColumnHeaderStyle>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="Background" Value="#2A2A2A"/>
                                            <Setter Property="Foreground" Value="#90CAF9"/>
                                            <Setter Property="Padding" Value="5,4"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="FontSize" Value="10"/>
                                        </Style>
                                    </DataGrid.ColumnHeaderStyle>
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Код" Width="50"/>
                                        <DataGridTextColumn Header="Инструмент" Width="70"/>
                                        <DataGridTextColumn Header="% измен." Width="60"/>
                                    </DataGrid.Columns>
                                    <DataGrid.RowStyle>
                                        <Style TargetType="DataGridRow">
                                            <Setter Property="Background" Value="#B71C1C"/>
                                        </Style>
                                    </DataGrid.RowStyle>
                                </DataGrid>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>

            <!-- ROW 1: 2 ПОД-КОЛОНКИ -->
            <Grid Grid.Row="1" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="30*"/>
                </Grid.ColumnDefinitions>

                <!-- КОЛОНКА 1.1: ПОРТФЕЛЬ -->
                <!-- 💼 ПОРТФЕЛЬ (стиль торгового терминала) -->
                <Border Grid.Column="0" Background="#1E1E1E" CornerRadius="4">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <!-- Вкладки и кнопки -->
                            <RowDefinition Height="*"/>
                            <!-- Таблица -->
                            <RowDefinition Height="Auto"/>
                            <!-- Футер (итоги) -->
                        </Grid.RowDefinitions>

                        <!-- ═══════════════════════════════════════════════════════════ -->
                        <!--  ВЕРХНЯЯ ПАНЕЛЬ: Вкладки и кнопки                           -->
                        <!-- ═══════════════════════════════════════════════════════════ -->
                        <Grid Grid.Row="0" Margin="10,10,10,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Вкладки -->
                            <StackPanel Orientation="Horizontal">
                                <Border Background="#1976D2" CornerRadius="3,3,0,0" Padding="15,5">
                                    <TextBlock Text="Все позиции" Foreground="White" FontSize="12"/>
                                </Border>
                                <Border Background="#2A2A2A" CornerRadius="3,3,0,0" Padding="15,5" Margin="2,0,0,0">
                                    <TextBlock Text="Открытые" Foreground="#B0B0B0" FontSize="12"/>
                                </Border>
                                <Border Background="#2A2A2A" CornerRadius="3,3,0,0" Padding="15,5" Margin="2,0,0,0">
                                    <TextBlock Text="На дату" Foreground="#B0B0B0" FontSize="12"/>
                                </Border>
                            </StackPanel>

                            <!-- Кнопки действий -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Border Background="#C62828" CornerRadius="3" Padding="10,5" Margin="2,0">
                                    <TextBlock Text="Закрыть" Foreground="White" FontSize="11"/>
                                </Border>
                                <Border Background="#F57C00" CornerRadius="3" Padding="10,5" Margin="2,0">
                                    <TextBlock Text="Перевернуть" Foreground="White" FontSize="11"/>
                                </Border>
                                <Border Background="#C62828" CornerRadius="3" Padding="10,5" Margin="2,0">
                                    <TextBlock Text="Закрыть всё" Foreground="White" FontSize="11"/>
                                </Border>
                            </StackPanel>
                        </Grid>

                        <!-- ═══════════════════════════════════════════════════════════ -->
                        <!--  ТАБЛИЦА ПОЗИЦИЙ                                            -->
                        <!-- ═══════════════════════════════════════════════════════════ -->
                        <DataGrid Grid.Row="1"
                                  AutoGenerateColumns="False"
                                  HeadersVisibility="Column"
                                  Background="#1E1E1E"
                                  RowBackground="#1E1E1E"
                                  AlternatingRowBackground="#252525"
                                  Foreground="White"
                                  BorderThickness="0"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#404040"
                                  VerticalGridLinesBrush="#404040"
                                  FontSize="12"
                                  FontFamily="Consolas"
                                  CanUserAddRows="False"
                                  IsReadOnly="True"
                                  Margin="10,0,10,0">

                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#2A2A2A"/>
                                    <Setter Property="Foreground" Value="#B0B0B0"/>
                                    <Setter Property="Padding" Value="8,6"/>
                                    <Setter Property="FontWeight" Value="Normal"/>
                                    <Setter Property="BorderThickness" Value="0,0,1,0"/>
                                    <Setter Property="BorderBrush" Value="#404040"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>

                            <DataGrid.Columns>
                                <!-- Инструмент -->
                                <DataGridTextColumn Header="Инструмент" 
                                    Binding="{Binding Instrument}" 
                                    Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="White"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Позиция -->
                                <DataGridTextColumn Header="Позиция" 
                                    Binding="{Binding Position}" 
                                    Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Цена -->
                                <DataGridTextColumn Header="Цена" 
                                    Binding="{Binding Price}" 
                                    Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Стоимость -->
                                <DataGridTextColumn Header="Стоимость" 
                                    Binding="{Binding Value}" 
                                    Width="80">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Нереал. P/L -->
                                <DataGridTextColumn Header="Нереал. P/L" 
                                    Binding="{Binding UnrealizedPL}" 
                                    Width="90">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsProfit}" Value="True">
                                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsProfit}" Value="False">
                                                    <Setter Property="Foreground" Value="#F44336"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- НПриб.% -->
                                <DataGridTextColumn Header="НПриб.%" 
                                    Binding="{Binding ProfitPercent}" 
                                    Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsProfit}" Value="True">
                                                    <Setter Property="Foreground" Value="#4CAF50"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsProfit}" Value="False">
                                                    <Setter Property="Foreground" Value="#F44336"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- В покупке -->
                                <DataGridTextColumn Header="В покупке" 
                                    Binding="{Binding InBuy}" 
                                    Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#B0B0B0"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- В продаже -->
                                <DataGridTextColumn Header="В продаже" 
                                    Binding="{Binding InSell}" 
                                    Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#B0B0B0"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>

                            <!-- Стили строк (зелёные/красные) -->
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsProfit}" Value="True">
                                            <Setter Property="Background" Value="#1B5E20"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsProfit}" Value="False">
                                            <Setter Property="Background" Value="#B71C1C"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>

                        <!-- ═══════════════════════════════════════════════════════════ -->
                        <!--  ФУТЕР: Итоговая панель (с Общей прибылью)                  -->
                        <!-- ═══════════════════════════════════════════════════════════ -->
                        <Border Grid.Row="2" 
                                Background="#2A2A2A" 
                                BorderBrush="#404040"
                                BorderThickness="0,1,0,0"
                                Margin="10,5,10,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <!-- 🆕 Общая прибыль -->
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Свободно -->
                                <StackPanel Orientation="Horizontal" Margin="10,5">
                                    <TextBlock Text="Свободно: " Foreground="#B0B0B0" FontSize="11"/>
                                    <TextBlock Text="354,15" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                </StackPanel>

                                <!-- Обеспечение -->
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="15,5">
                                    <TextBlock Text="Обеспечение: " Foreground="#B0B0B0" FontSize="11"/>
                                    <TextBlock Text="439,19" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                </StackPanel>

                                <!-- Прибыль дня -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="15,5">
                                    <TextBlock Text="Прибыль дня: " Foreground="#B0B0B0" FontSize="11"/>
                                    <TextBlock Text="1,27" Foreground="#4CAF50" FontSize="11" FontWeight="Bold"/>
                                </StackPanel>

                                <!-- 🆕 ОБЩАЯ ПРИБЫЛЬ -->
                                <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="15,5">
                                    <TextBlock Text="Общая прибыль: " Foreground="#B0B0B0" FontSize="11"/>
                                    <TextBlock Text="+4,520" Foreground="#4CAF50" FontSize="11" FontWeight="Bold"/>
                                </StackPanel>

                                <!-- Прибыль % (сдвинут на колонку) -->
                                <StackPanel Grid.Column="4" Orientation="Horizontal" Margin="15,5">
                                    <TextBlock Text="Прибыль %: " Foreground="#B0B0B0" FontSize="11"/>
                                    <TextBlock Text="0,29" Foreground="#4CAF50" FontSize="11" FontWeight="Bold"/>
                                </StackPanel>

                                <!-- Деньги (сдвинут на колонку) -->
                                <StackPanel Grid.Column="5" Orientation="Horizontal" Margin="15,5">
                                    <TextBlock Text="Деньги: " Foreground="#B0B0B0" FontSize="11"/>
                                    <TextBlock Text="8,33" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>

                <!-- РАЗДЕЛИТЕЛЬ -->
                <Border Grid.Column="1" Background="#404040" Margin="0,5"/>

                <!-- КОЛОНКА 1.2: 📅 Календарь -->
                <Border Grid.Column="2" Background="#1E1E1E" CornerRadius="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" 
                                   Text="📅 Календарь" 
                                   FontSize="14" FontWeight="Bold" 
                                   Foreground="White"
                                   Margin="15,12,15,10"/>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" Margin="10,0,10,10">
                            <StackPanel>
                                <!-- Событие 1 -->
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="14:30" Foreground="#90CAF9" FontSize="10"/>
                                    <TextBlock Grid.Column="1" Text="CPI (Инфляция)" Foreground="White" FontSize="11"/>
                                    <Ellipse Grid.Column="2" Width="8" Height="8" Fill="#F44336" HorizontalAlignment="Center"/>
                                </Grid>
                                <!-- Событие 2 -->
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="16:00" Foreground="#90CAF9" FontSize="10"/>
                                    <TextBlock Grid.Column="1" Text="NFP (Занятость)" Foreground="White" FontSize="11"/>
                                    <Ellipse Grid.Column="2" Width="8" Height="8" Fill="#F44336" HorizontalAlignment="Center"/>
                                </Grid>
                                <!-- Событие 3 -->
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="21:00" Foreground="#90CAF9" FontSize="10"/>
                                    <TextBlock Grid.Column="1" Text="Fed Rate" Foreground="White" FontSize="11"/>
                                    <Ellipse Grid.Column="2" Width="8" Height="8" Fill="#FF9800" HorizontalAlignment="Center"/>
                                </Grid>
                                <!-- Событие 4 -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="12:00" Foreground="#90CAF9" FontSize="10"/>
                                    <TextBlock Grid.Column="1" Text="ВВП (ЕС)" Foreground="White" FontSize="11"/>
                                    <Ellipse Grid.Column="2" Width="8" Height="8" Fill="#FFC107" HorizontalAlignment="Center"/>
                                </Grid>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

    </Grid>
</Page>
